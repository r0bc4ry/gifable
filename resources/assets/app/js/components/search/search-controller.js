'use strict';

angular.module('gifable.app.controllers')
    .controller('SearchController', [
        '$scope',
        function(
            $scope
        ) {
            // TODO Implement
        }
    ]);
